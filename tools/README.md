# tools/

Chequeo público opcional de URLs (status, `h1`, meta description). No es el producto que se vende.

**Sí:** `node tools/hygiene-check.mjs` → `output/hygiene-report.json` (vía `curl`).

**No:** app, login, scraper de formularios.
