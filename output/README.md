# output/

- `hygiene-report.json` — chequeo público (`node tools/hygiene-check.mjs`).
- `metrics`: cifras de cotizador solo si hay CSV de CAFSA en `data/` (aún no: `null` + `warning`).
- `section9`: los seis puntos del done, leídos del HTML público (`pass` sigue en `false` hasta publicar en WP).

**No:** dashboards de demostración ni informes legales.
